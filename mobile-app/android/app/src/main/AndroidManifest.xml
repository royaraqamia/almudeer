<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================ -->
    <!-- CORE FUNCTIONALITY PERMISSIONS -->
    <!-- ============================================ -->

    <!-- Network & Internet -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <!-- Camera Hardware -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <!-- Audio Recording (Voice Messages) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>

    <!-- Storage (Android 9 and below - scoped storage on Android 10+) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <!-- Contacts (For sharing/customer features) -->
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <!-- ============================================ -->
    <!-- NOTIFICATIONS & ALERTS -->
    <!-- ============================================ -->

    <!-- Push Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Boot Completed (Restore notifications after reboot) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Full Screen Intent (Call/Alarm UI) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Exact Alarms (Scheduled notifications) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Vibrate (Notification feedback) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Wake Lock (Keep CPU awake during operations) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ============================================ -->
    <!-- FOREGROUND SERVICES -->
    <!-- ============================================ -->

    <!-- General Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Data Sync (File transfers, background operations) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Microphone (Voice message recording in background) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Media Playback (Audio service) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- ============================================ -->
    <!-- NEARBY CONNECTIONS (Device-to-Device) -->
    <!-- ============================================ -->

    <!-- Bluetooth (Android 10 and below) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Bluetooth (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Location (Required for Bluetooth scanning on Android 11 and below) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- WiFi Nearby (Android 13+) -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" android:usesPermissionFlags="neverForLocation" />

    <!-- ============================================ -->
    <!-- REMOVED PERMISSIONS (Google Play Compliance) -->
    <!-- ============================================ -->
    <!--
    The following permissions were removed to comply with Google Play policies:

    ❌ MANAGE_EXTERNAL_STORAGE - Not required for core functionality
       → Use Storage Access Framework instead

    ❌ SYSTEM_ALERT_WINDOW - Not required for core functionality
       → Use full-screen activities instead

    ❌ MANAGE_MEDIA - Not required for core functionality
       → Use Media Store API instead

    ❌ PACKAGE_USAGE_STATS - Protected permission, not core functionality
       → No alternative needed

    ❌ REQUEST_INSTALL_PACKAGES - Triggers security warning, not core functionality
       → Use file opener for downloaded files instead

    If any of these permissions are absolutely required, you must:
    1. Complete Google Play Permissions Declaration Form
    2. Provide video demonstration
    3. Wait 1-4 weeks for approval
    4. Be prepared for rejection

    See: docs/PERMISSIONS_SECURITY_AUDIT.md for full details
    -->

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:extractNativeLibs="false"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="false"
        android:fullBackupContent="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />

            <!-- Main Launcher Intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- File Viewing Intent (Specific media types for Google Play compliance) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Images -->
                <data android:mimeType="image/*" />
                <!-- Videos -->
                <data android:mimeType="video/*" />
                <!-- Audio -->
                <data android:mimeType="audio/*" />
                <!-- Documents -->
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />
                <data android:mimeType="application/msword" />
                <data android:mimeType="application/vnd.ms-excel" />
                <data android:mimeType="application/vnd.ms-powerpoint" />
                <!-- Text -->
                <data android:mimeType="text/plain" />
                <data android:mimeType="text/html" />
            </intent-filter>

            <!-- HTTP/HTTPS Links (Web URLs) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="http" />
                <data android:scheme="https" />
            </intent-filter>

            <!-- Share Intent (Receive shared content - Specific media types for Google Play compliance) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <!-- Images -->
                <data android:mimeType="image/*" />
                <!-- Videos -->
                <data android:mimeType="video/*" />
                <!-- Audio -->
                <data android:mimeType="audio/*" />
                <!-- Documents -->
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.presentationml.presentation" />
                <!-- Text -->
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Deep Link: Auto-Login -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="almudeer" android:host="login" />
            </intent-filter>

            <!-- Deep Links: QR Scanner & App Sections -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="almudeer" android:host="qr-scanner" />
                <data android:scheme="almudeer" android:host="login" />
                <data android:scheme="almudeer" android:host="settings" />
                <data android:scheme="almudeer" android:host="inbox" />
                <data android:scheme="almudeer" android:host="customers" />
                <data android:scheme="almudeer" android:host="tasks" />
                <data android:scheme="almudeer" android:host="library" />
            </intent-filter>
        </activity>

        <!-- Flutter Embedding Metadata -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Firebase Messaging Icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <!-- WebView Security Hardening (Disable file/content access, geolocation) -->
        <meta-data
            android:name="android.webkit.WebView.EnableSafeBrowsing"
            android:value="true" />
        <meta-data
            android:name="android.webkit.WebView.MetricsEnabled"
            android:value="false" />

        <!-- File Provider (Secure file sharing) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <!-- Transfer Foreground Service (File transfers) -->
        <service
            android:name=".TransferForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <!-- Audio Service (Media playback) -->
        <!-- Intentionally exported for media playback controls (headset, Android Auto, etc.) -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            tools:node="merge">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Media Button Receiver (Headset controls) -->
        <!-- Intentionally exported for headset button controls -->
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true"
            tools:node="merge">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Local Notifications Receivers -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- NOTE: NotificationListener service was removed for Google Play compliance -->
        <!-- This permission requires special approval and is only for notification manager apps -->

    </application>

    <!-- App Queries (Android 11+ Package Visibility) -->
    <queries>
        <!-- Text Processing -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Web Browsing -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>
